# ── PDF extraction ────────────────────────────────────────────────────────────
pdfplumber>=0.10.0
pypdf>=3.0.0
pytesseract>=0.3.10        # OCR fallback (needs system tesseract-ocr)
pdf2image>=1.16.0          # OCR fallback (needs system poppler-utils)
Pillow>=10.0.0             # OCR fallback

# ── NLP / embeddings ─────────────────────────────────────────────────────────
spacy>=3.7.0               # patent_parser NER (run: python -m spacy download en_core_web_sm)
sentence-transformers>=2.6.0  # entity_resolver embeddings
faiss-cpu>=1.7.4           # entity_resolver vector search

# ── Graph ─────────────────────────────────────────────────────────────────────
networkx>=3.2.0            # github_parser + kg_builder + hop_reasoner

# ── Data / validation ────────────────────────────────────────────────────────
pydantic>=2.0.0            # whitepaper_parser schema validation (optional)

# ── LLM API ──────────────────────────────────────────────────────────────────
anthropic>=0.25.0          # question_gen (set ANTHROPIC_API_KEY env var)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0